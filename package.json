{
  "name": "coretext",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension that prints the current page to the console.",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "typecheck:scripts": "tsc -p scripts",
    "gemini": "node scripts/testGemini.mts"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
